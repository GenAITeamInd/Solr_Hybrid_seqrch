Use the solr.py file for uploading the documents into the solr core
Use the fix_solr_schema.py to fix the schema of the SOLR core to match the dimensions of the model that you are using for embedding
app.py contains the main code with frontend made using streamlit
promt_template.py is a part of app.py to assist in keyword search
